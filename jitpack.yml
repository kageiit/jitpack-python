install:
  - wget <url> -O python-2.7.tar.gz
  - SHA=$(git rev-parse HEAD)
  - mvn install:install-file -Dfile=python-2.7.tar.gz -DgroupId="$GROUP" -DartifactId="$ARTIFACT" -Dversion="$SHA" -Dpackaging="tar.gz" -DgeneratePom=true